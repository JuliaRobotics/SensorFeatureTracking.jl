package vicon;

struct body_t
{
 	int64_t utime;              // utime that the measurement took place

    double pos[3];              // position x,y,z in meters
 
    double orientation[4];      // a quaterion.
    							// rotate vector in body coordinate frame 
                                // (X-forward, Z-up) by this to get that vector
                                // in local frame		
}
